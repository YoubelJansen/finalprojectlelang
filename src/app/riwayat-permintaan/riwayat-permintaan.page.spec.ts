import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RiwayatPermintaanPage } from './riwayat-permintaan.page';

describe('RiwayatPermintaanPage', () => {
  let component: RiwayatPermintaanPage;
  let fixture: ComponentFixture<RiwayatPermintaanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RiwayatPermintaanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
